# --finalProject
---------------------
- All coding files are saved in src folder 
- All input data is saved in data_field folder
- If terminal outputs "system("clear") is not defined", suppose that "system("clear")" is exclusively used in macos terminal. To ensure the system cleans the terminal properly in windowns terminal, use "system("cls")"
